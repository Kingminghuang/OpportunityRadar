{
  "name": "opportunity-radar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build && node scripts/generate-markdown-archives.mjs --out dist/archives",
    "build:pages": "astro check && astro build --site \"$SITE_URL\" --base \"$BASE_PATH\" && node scripts/generate-markdown-archives.mjs --out dist/archives",
    "test": "vitest run"
  },
  "dependencies": {
    "astro": "^7.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^26.1.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
